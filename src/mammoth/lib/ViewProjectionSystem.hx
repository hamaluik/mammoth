package mammoth.lib;

import edge.ISystem;
import mammoth.lib.Camera;
import mammoth.lib.ModelMatrix;
import mammoth.lib.ViewProjectionMatrix;

class ViewProjectionSystem implements ISystem {
	public function update(m:ModelMatrix, c:Camera, vpm:ViewProjectionMatrix) {
		// TODO
	}
}